interface:
  display_name: "Produce Product Videos"
  short_description: "Generate TikTok-style editable product footage"
  default_prompt: "Generate multiple 15s 9:16 editable product footage variants in TikTok creator/livestream daily-use style, keep all versions lifestyle-driven, use raw references as the primary source for material realism, use keyframes and image analysis only for feature and scene ideas, maximize diversity across functional angles, avoid voiceover and subtitles, allow concise PPT-style text overlays, keep prompt body fully English (Chinese allowed only inside image reference names like @filename), filter out noisy tokens, convert references to base64, and submit tasks to the Seedance mock gateway."
